// import mysql from 'mysql';

// Set database connection credentials 
const poolSqlConfig = {
  host: 'localhost',
  user: 'root',
  password: 'root',
  database: 'api',
};

// Create a MySQL pool 
// const pool = mysql.createPool(console.log(poolSqlConfig););

// Export the pool 
export default poolSqlConfig;